# R-NNNN: Rule title

- Status: enforced
- Source: — <!-- ADR-NNNN, when this rule follows from a recorded decision -->

<!-- HOW TO FILL THIS IN — delete these comments as you go.

     Title:  state the constraint, not the topic. "No secrets in the
             repository", not "Secrets". A reviewer should be able to cite the
             title alone and be understood.
-->

## Rule

<!-- One imperative sentence. No hedging: "never", "always", "must".
     If you need "should" or "generally", this belongs in guidelines instead.

     Be specific enough to be checkable. "Validate input" is not checkable;
     "validate every external input at the boundary before it reaches domain
     code" is. -->

## Why

<!-- The consequence of breaking it, concretely. Not "for security" — what
     actually goes wrong, and why it is not recoverable by ordinary means.
     This is what stops the rule being deleted by someone who finds it
     inconvenient at 2am. -->

## Enforcement

<!-- What checks this, by name: CI job, lint rule, pre-commit hook, branch
     protection, code review, manual audit on a schedule.

     If the answer is "nothing", the status is `advisory`, not `enforced`. -->
