# GL-NNNN: Guideline title

- Status: active
- Source: — <!-- ADR-NNNN, when this follows from a recorded decision -->

<!-- HOW TO FILL THIS IN — delete these comments as you go. -->

## Scope

<!-- Where this applies: which languages, layers, modules, or kinds of change.
     A guideline that claims to apply everywhere usually applies nowhere,
     because nobody knows when to invoke it. -->

## Guideline

<!-- What to do by default. Imperative and specific.
     Use "prefer" and "avoid" rather than "must" and "never" — the hard words
     belong to rules. -->

## Rationale

<!-- Why this default and not the obvious alternative. One or two sentences.
     If you cannot say why, the convention may just be a habit — which is
     fine, but say that, so a future reader knows the bar for changing it. -->

## Examples

<!-- The part people actually read, and the part an agent pattern-matches on.
     Keep both snippets minimal and about one thing. -->

Prefer:

```
```

Avoid:

```
```
